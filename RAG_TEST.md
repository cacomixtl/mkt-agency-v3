## RAG pipeline test.

### Mock up interactions of 10 different users, add them tou our PostgreSQL + pgvector instance within our system. 

#### Simple conversation with enough retrievable and verfiable (different across each user. UUID, perhaps? Tokenized UUID? No, its subobtimal) objects throughout the tokens embedded on each thread_id. 

#### The idea is to test the storage quality and performance of our current architecture.
