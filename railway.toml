[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c'uvicorn main_v3:app --host 0.0.0.0 --port ${PORT:-8000}'"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"

[volumes]
static_data = "/app/static"
